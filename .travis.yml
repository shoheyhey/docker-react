language: generic 

sudo: required
services:
    - docker

before_install:
    - docker build -t shoheyhey/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true shoheyhey/docker-react npm run test